# homebrew-finance (Deprecated)

> **This tap has moved to [itomek/homebrew-tap](https://github.com/itomek/homebrew-tap).**

## Migration

```bash
brew untap itomek/finance
brew tap itomek/tap
brew install finance
```

The `finance` formula is now maintained in the shared tap alongside other itomek tools.
